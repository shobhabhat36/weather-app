import React from 'react'
import WeatherBanner from './WeatherBanner';

const WeatherWidget = () => {
    return (
        <div>
            <WeatherBanner />
        </div>
    );
};

export default WeatherWidget;